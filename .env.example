# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=8080
ENV=development
GIN_MODE=debug

# ============================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================
# PostgreSQL connection details
DB_HOST=localhost
DB_PORT=5432
DB_USER=telemoz
DB_PASSWORD=password
DB_NAME=telemoz_db
DB_SSLMODE=disable

# ============================================
# JWT CONFIGURATION (REQUIRED)
# ============================================
# Generate a secure random string for production (min 32 characters)
# You can generate one using: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production-min-32-chars
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=168h

# ============================================
# TRACCAR CONFIGURATION (REQUIRED for tracking)
# ============================================
# Traccar server URL (default installation: http://localhost:8082)
TRACCAR_URL=http://localhost:8082
TRACCAR_USERNAME=admin
TRACCAR_PASSWORD=admin

# ============================================
# GOOGLE MAPS API (REQUIRED for location features)
# ============================================
# Get your API key from: https://console.cloud.google.com/
# Enable: Geocoding API, Distance Matrix API
GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# ============================================
# SMS PROVIDER (OPTIONAL - Twilio)
# ============================================
# Leave empty to use no-op provider (for development)
SMS_PROVIDER=twilio
SMS_API_KEY=your-twilio-account-sid
SMS_API_SECRET=your-twilio-auth-token
SMS_FROM_NUMBER=+1234567890

# ============================================
# VOICE CALL PROVIDER (OPTIONAL - Twilio)
# ============================================
# Leave empty to use no-op provider (for development)
VOICE_PROVIDER=twilio
VOICE_API_KEY=your-twilio-account-sid
VOICE_API_SECRET=your-twilio-auth-token

# ============================================
# FIREBASE (OPTIONAL - for push notifications)
# ============================================
# Download service account JSON from Firebase Console
# Place the file in the project root or specify full path
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CREDENTIALS_PATH=./firebase-credentials.json

# ============================================
# CORS CONFIGURATION (REQUIRED)
# ============================================
# Add your frontend URLs (comma-separated)
# React Native dev server typically runs on port 19006
# Add production URLs when deploying
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:19006,http://localhost:8081